// app start time
export const time = Math.round( new Date().getTime() / 1000 );

// user`ticket
export const auth = '';

// user`info
export const user = {
	nickName: '',
	sex: 0,
	headImgUrl: ''
};
